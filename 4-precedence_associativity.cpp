/**
 * Learning about Operators in C++
 * Jenny's Lectures
*/

#include <iostream>

using namespace std;

int main(int argc, const char** argv) {

	/**
	 * Precedence and associativity in
	 * is a concept of priorty in a multi-operator statement
	 * Just like (BODMAS)
	 * Checkout C++ precedence table
	 * An operator of higher precedence will be evaluated first
	 * Associativity is Left to Right
	 * the Comma operator ( lower than = operator. but in ( 1, 2), it's evaluated first before the assignment)
	*/
	/*int result = 2 + 3 | 12 / 6 - 1 << 1 && 6 % 7 / 3 * 5;*/

	

	return 0;
}

