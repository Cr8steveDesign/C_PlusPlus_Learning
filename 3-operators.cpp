/**
 * Learning about Operators in C++
 * Jenny's Lectures
*/

#include <iostream>

using namespace std;

int main(int argc, const char** argv) {

	/**Operators in C++
	 * Types of Operators
	 * Arithmetic 	[+ - * / % ++ --]
	 * Relational 	[< > != == >= <=]
	 * Logical		[ &&  ||  !]
	 * Bitwise		[ & | ^ ~ << >>]
	 * Assignment	[ = += -= /= *=]
	 * Misc			[]
	*/

	/**
	 * Logical Operators can be used to perform short circuit operations
	*/
	/**
	 * *Still the same principles with normal C
	 */

	/**
	 * Tenary Operators
	 * (condition) ? expression1 : expression2
	*/

	/**
	 * sizeof() - Operator for getting the size of a variable or type
	 * & - Used to get the address of a variable
	 * * - Used in declaration and dereferencing and address
	 * (type) - For casting one type to another
	 * float weight = 5.60;  cout << (int)weight; //ouputs 5
	*/

	return 0;
}
